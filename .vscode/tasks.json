{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build (web server)",
      "type": "process",
      "command": "dotnet",
      "args": ["build", "${workspaceFolder}/web/server/server.csproj"],
      "problemMatcher": "$msCompile",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
