{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build (server)",
      "type": "process",
      "command": "dotnet",
      "args": ["build", "${workspaceFolder}/server/server.csproj"],
      "problemMatcher": "$msCompile",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
